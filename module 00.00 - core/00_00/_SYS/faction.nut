::mods_hookBaseClass("factions/faction", function ( o )
{
    // function update( _ignoreDelay = false, _isNewCampaign = false )

    //Settlements

    // add function to get capital -> 3 contracts, top contracts
    // add function to get towns -> 2 contracts
    // add function to get villages -> 1 contract

    // special/story contracts depend
});